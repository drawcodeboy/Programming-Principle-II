#include <stdio.h>
#include <conio.h>

int main(void){
    int ch;
    /*
    while((ch = getchar()) != EOF){
        putchar(ch);
    }
    */
    while((ch = getche()) != 'q'){
        putch(ch);
    }
    return 0;
}